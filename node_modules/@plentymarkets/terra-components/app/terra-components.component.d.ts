import { OnInit, ViewContainerRef } from '@angular/core';
import { LocaleService, Translation, TranslationService } from 'angular-l10n';
export declare class TerraComponentsComponent extends Translation implements OnInit {
    private viewContainerRef;
    locale: LocaleService;
    translation: TranslationService;
    private _viewContainerRef;
    constructor(viewContainerRef: ViewContainerRef, locale: LocaleService, translation: TranslationService);
    ngOnInit(): void;
}
