import { TerraUploadItem } from './terra-upload-item';
import { Observable } from 'rxjs/Observable';
export declare type UploadQueueUrlFactory = (storageKey: string) => string;
export declare class TerraUploadQueue {
    private _uploadUrl;
    private _uploadMethod;
    private items;
    private size;
    progress: Observable<number>;
    inProgress: Promise<void>;
    private _progressListeners;
    private _progressValue;
    constructor(_uploadUrl: string | UploadQueueUrlFactory, _uploadMethod?: 'GET' | 'POST' | 'DELETE' | 'PUT');
    add(item: TerraUploadItem): TerraUploadQueue;
    remove(item: TerraUploadItem): TerraUploadQueue;
    startUpload(): Promise<void>;
    private uploadAllItems();
    private uploadItem(item);
    private onProgress();
    private parseHeaders(headers);
    private getUploadUrl(storageKey);
}
