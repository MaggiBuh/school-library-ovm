import { TerraStorageObjectList } from './model/terra-storage-object-list';
import { Observable } from 'rxjs/Observable';
import { TerraUploadItem } from './model/terra-upload-item';
import { TerraImageMetadata } from './model/terra-image-metadata.interface';
import { TerraUploadQueue } from './model/terra-upload-queue';
import { TerraBaseService } from '../../../';
export declare abstract class TerraBaseStorageService extends TerraBaseService {
    abstract name: string;
    abstract isImagePreviewEnabled: boolean;
    abstract queue: TerraUploadQueue;
    abstract getStorageList(): Observable<TerraStorageObjectList>;
    abstract createDirectory(path: string): Observable<any>;
    abstract uploadFiles(files: FileList | File[], path: string): TerraUploadItem[];
    abstract deleteFiles(keys: string[]): Observable<any>;
    prepareKey(value: string, isName?: boolean, isDirectory?: boolean): string;
}
export declare abstract class TerraBaseMetadataStorageService extends TerraBaseStorageService {
    abstract getMetadata(key: string): Observable<TerraImageMetadata>;
    abstract updateMetadata(key: string, metadata: TerraImageMetadata): Observable<any>;
}
export declare abstract class TerraBasePrivateStorageService extends TerraBaseStorageService {
    abstract downloadFile(key: string): void;
}
