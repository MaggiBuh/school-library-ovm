import { NgZone } from '@angular/core';
import { BehaviorSubject } from 'rxjs/BehaviorSubject';
export declare class TerraFileBrowserService {
    private zone;
    private _dropzones;
    private _globalListenersDefined;
    private _dragenterTarget;
    isDragActive: BehaviorSubject<boolean>;
    constructor(zone: NgZone);
    addDropzone(dropzone: HTMLElement): void;
    removeDropzone(dropzone: HTMLElement): void;
    private isDropzone(element);
    private setupGlobalListeners();
}
