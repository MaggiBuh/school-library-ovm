import { EventEmitter } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { TerraPlacementEnum } from '../../../../';
export declare class TerraToggleComponent implements ControlValueAccessor {
    inputIsSmall: boolean;
    inputIsLarge: boolean;
    inputIsDisabled: boolean;
    inputIcon: string;
    inputIsAlignRight: boolean;
    inputIsHidden: boolean;
    inputTooltipText: string;
    inputTooltipPlacement: TerraPlacementEnum;
    deactivated: EventEmitter<any>;
    activated: EventEmitter<any>;
    toggled: EventEmitter<boolean>;
    private _isActive;
    constructor();
    private toggle();
    writeValue(value: boolean): void;
    registerOnChange(fn: (_: any) => void): void;
    registerOnTouched(fn: () => void): void;
    private onTouchedCallback;
    private onChangeCallback;
}
