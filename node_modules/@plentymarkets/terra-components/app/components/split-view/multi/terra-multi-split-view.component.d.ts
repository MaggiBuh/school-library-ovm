import { NgZone, OnDestroy, OnInit } from '@angular/core';
import { TerraMultiSplitViewConfig } from './data/terra-multi-split-view.config';
import { TerraMultiSplitViewDetail } from './data/terra-multi-split-view-detail';
import { TerraMultiSplitViewInterface } from './data/terra-multi-split-view.interface';
import { Router } from '@angular/router';
export declare class TerraMultiSplitViewComponent implements OnDestroy, OnInit {
    private zone;
    private _router;
    inputConfig: TerraMultiSplitViewConfig;
    inputShowBreadcrumbs: boolean;
    inputComponentRoute: string;
    onWindowResize(): void;
    private _breadCrumbsPath;
    private modules;
    static ANIMATION_SPEED: number;
    private resizeTimeout;
    private splitViewId;
    constructor(zone: NgZone, _router: Router);
    ngOnDestroy(): void;
    ngOnInit(): void;
    private addToModulesIfNotExist(view);
    private setSelectedView(view);
    private updateBreadCrumbs();
    updateViewport(view: TerraMultiSplitViewInterface, skipAnimation?: boolean): void;
    private rebuildModules(view, module);
    private removeFromModules(view);
    protected getModuleOfView(view: any): TerraMultiSplitViewDetail;
    private getHierarchyLevelOfView(view);
    private resizeViewAndModule(view);
    private removeView(view, event);
    private routeExists(route);
    private getLastSelectedOfModule(module);
}
