import { OnChanges, OnDestroy, SimpleChanges } from '@angular/core';
import { TerraSplitViewInterface } from './data/terra-split-view.interface';
/** @deprecated - please use `TerraMultiSplitViewComponent` instead */
export declare class TerraSplitViewComponent implements OnChanges, OnDestroy {
    inputModules: Array<TerraSplitViewInterface>;
    inputShowBreadcrumbs: boolean;
    inputUpdateViewport: boolean;
    private _breadCrumbsPath;
    static ANIMATION_SPEED: number;
    constructor();
    ngOnDestroy(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private updateViewport(id);
}
