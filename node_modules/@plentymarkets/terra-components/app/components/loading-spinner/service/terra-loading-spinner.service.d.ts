import { NgZone } from '@angular/core';
import { Observable } from 'rxjs';
/**
 * @author mscharf
 */
export declare class TerraLoadingSpinnerService {
    private zone;
    observable: Observable<boolean>;
    private isLoading;
    private _subscriber;
    constructor(zone: NgZone);
    start(): void;
    stop(): void;
}
