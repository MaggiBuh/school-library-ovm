/// <reference types="interact.js" />
import { ChangeDetectorRef, ElementRef, EventEmitter, OnInit } from '@angular/core';
export declare class TerraSliderComponent implements OnInit {
    private element;
    private changeDetector;
    inputValue: number;
    inputValueChange: EventEmitter<number>;
    inputName: string;
    inputMin: number;
    inputMax: number;
    inputInterval: number;
    inputPrecision: number;
    inputShowMinMax: boolean;
    inputShowTicks: boolean;
    inputIsDisabled: boolean;
    private sliderBarElement;
    constructor(element: ElementRef, changeDetector: ChangeDetectorRef);
    handlePosition: number;
    private readonly grid;
    ngOnInit(): void;
    onDrag(event: Interact.InteractEvent): void;
    onBarClicked(event: MouseEvent): void;
    private moveToPosition(position);
    getTicks(): {
        position: number;
        caption: number;
    }[];
}
