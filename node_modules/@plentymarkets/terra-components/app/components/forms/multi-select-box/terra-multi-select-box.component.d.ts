import { EventEmitter, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { TerraMultiSelectBoxValueInterface } from './data/terra-multi-select-box-value.interface';
import { TerraCheckboxComponent } from '../checkbox/terra-checkbox.component';
import { TranslationService } from 'angular-l10n';
/** @deprecated - please use `TerraMultiCheckBoxComponent` instead */
export declare class TerraMultiSelectBoxComponent implements OnInit, OnChanges {
    translation: TranslationService;
    viewChildHeaderCheckbox: TerraCheckboxComponent;
    inputIsDisabled: boolean;
    inputIsError: boolean;
    inputValueList: Array<TerraMultiSelectBoxValueInterface>;
    inputName: string;
    inputIsRequired: boolean;
    inputSelectedValueListChange: EventEmitter<any[]>;
    inputSelectedValueList: Array<any>;
    private _selectedValueList;
    private _isInit;
    private _langPrefix;
    constructor(translation: TranslationService);
    ngOnInit(): void;
    /**
     *
     * @param changes
     */
    ngOnChanges(changes: SimpleChanges): void;
    private primaryClicked();
    private onHeaderCheckboxChange(isChecked);
    private onValueCheckboxChange(isChecked, value);
    private changeValueState(isChecked, valueToChange);
    private extractSelectedValues(valueList);
}
