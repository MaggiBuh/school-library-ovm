import { ElementRef, EventEmitter, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { TerraSelectBoxValueInterface } from './data/terra-select-box.interface';
import { FormControl } from '@angular/forms';
export declare class TerraSelectBoxComponent implements OnInit, OnChanges {
    private elementRef;
    inputName: string;
    inputIsRequired: boolean;
    inputIsDisabled: boolean;
    inputIsSmall: boolean;
    inputOpenOnTop: boolean;
    inputTooltipText: string;
    inputTooltipPlacement: string;
    inputListBoxValues: Array<TerraSelectBoxValueInterface>;
    outputValueChanged: EventEmitter<TerraSelectBoxValueInterface>;
    inputSelectedValueChange: EventEmitter<TerraSelectBoxValueInterface>;
    isValid: boolean;
    private _value;
    private _selectedValue;
    private _toggleOpen;
    private _hasLabel;
    private _isInit;
    private clickListener;
    /**
     * @deprecated
     * @param value
     */
    inputSelectedValue: number | string;
    /**
     *
     * @param elementRef
     */
    constructor(elementRef: ElementRef);
    ngOnInit(): void;
    /**
     *
     * @param changes
     */
    ngOnChanges(changes: SimpleChanges): void;
    registerOnChange(fn: (_: any) => void): void;
    registerOnTouched(fn: () => void): void;
    /**
     *
     * Two way data binding by ngModel
     */
    private onTouchedCallback;
    private onChangeCallback;
    writeValue(value: any): void;
    value: any;
    private toggleOpen;
    /**
     *
     * @param event
     */
    private clickedOutside(event);
    private onClick(evt);
    /**
     *
     * @param value
     */
    private select(value);
    validate(formControl: FormControl): void;
}
