import { TerraTileBoxInterface } from '../box/data/terra-tile-box.interface';
/**
 * @author mkunze
 */
export declare class TerraTileBoxPanelComponent {
    inputTileBoxList: Array<TerraTileBoxInterface>;
    inputIsViewToggleable: boolean;
    selectedTileBoxList: Array<TerraTileBoxInterface>;
    viewStyle: boolean;
    private _draggedIndex;
    constructor();
    private onBoxClick(event, tile);
    private onDragStart(event, draggingTile);
    private onDragEnd(event, draggingTile);
    private onDragLeave(event, hoverTile);
    private onDrop(event, droppedTile);
    private onDragOver(event, hoverTile);
    private toggleView();
}
