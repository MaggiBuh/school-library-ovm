/// <reference types="interact.js" />
import { ElementRef, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { ResizeOptions } from './resizeOptions.interface';
import { InertiaOptions } from './inertiaOptions.interface';
import { RestrictOptions } from './restrictOptions.interface';
import { GridOptions } from './gridOptions.interface';
export declare class TerraResizableDirective implements OnChanges {
    private el;
    options: ResizeOptions;
    disabled: boolean;
    grid: false | GridOptions;
    restrict: RestrictOptions;
    inertia: boolean | InertiaOptions;
    onStart: EventEmitter<Interact.InteractEvent>;
    onMove: EventEmitter<Interact.InteractEvent>;
    onEnd: EventEmitter<Interact.InteractEvent>;
    private interactable;
    constructor(el: ElementRef);
    ngOnChanges(changes: SimpleChanges): void;
    private prepareImmutableInput(input);
    private init();
    private handleSnap(x, y);
}
