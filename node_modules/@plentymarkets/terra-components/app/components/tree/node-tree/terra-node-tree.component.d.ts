import { OnDestroy, OnInit } from '@angular/core';
import { TerraNodeTreeConfig } from './data/terra-node-tree.config';
import { TranslationService } from 'angular-l10n';
export declare class TerraNodeTreeComponent<D> implements OnDestroy, OnInit {
    private _translation;
    /**
     * @description The config to handle actions on tree or node.
     */
    inputConfig: TerraNodeTreeConfig<D>;
    /**
     * @description Shows the search box above the tree.
     */
    inputShowSearch: boolean;
    /**
     * @description Disables or enables the System Tree
     */
    isTreeDisabled: boolean;
    protected _searchValue: string;
    constructor(_translation: TranslationService);
    ngOnInit(): void;
    private handleVisibility(nodeList);
    private handleDefaultVisibility(nodeList);
    ngOnDestroy(): void;
    protected onChange(): void;
    private recursiveCheckList(list);
    private handleSearch(node, value);
    private handleNodeVisibility(node);
}
