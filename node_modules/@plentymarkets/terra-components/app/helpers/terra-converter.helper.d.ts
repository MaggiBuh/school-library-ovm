import { LocaleService } from 'angular-l10n';
export declare class TerraConverterHelper {
    /**
     * Automatically change the byte size to the corresponding range with the appropriate unit
     * @param {number} byteSize
     * @param {LocaleService} localeService
     * @returns {string}
     */
    convertAndFormatSize(byteSize: number, localeService: LocaleService): string;
    /**
     * @param {number} number
     * @returns {string}
     */
    getLocaleDecimalValue(number: number, locale: LocaleService): string;
}
