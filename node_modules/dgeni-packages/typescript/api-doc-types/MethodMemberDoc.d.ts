import { Declaration, Symbol } from 'typescript';
import { ContainerExportDoc } from './ContainerExportDoc';
import { MemberDoc } from './MemberDoc';
export declare class MethodMemberDoc extends MemberDoc {
    overloads: MethodMemberDoc[];
    readonly parameters: string[];
    readonly name: string;
    readonly anchor: string;
    readonly id: string;
    readonly aliases: string[];
    readonly typeParameters: string;
    constructor(containerDoc: ContainerExportDoc, symbol: Symbol, declaration: Declaration, basePath: string, namespacesToInclude: string[], isStatic: boolean, overloads?: MethodMemberDoc[]);
    private computeName();
    private computeAnchor();
    private computeAliases();
}
